# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/file_sharing_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=admin123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# File Storage Configuration
# Base directory; files are automatically routed into subfolders:
#   storage/images, storage/videos, storage/audio, storage/documents
file.upload-dir=storage

# Multipart upload limits (adjust as needed)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=200MB

# JWT Configuration
app.jwtSecret=SecretKeyToGenJWTsThisShouldBeLongAndComplexForSecurityReasons
app.jwtExpirationInMs=3600000

# PROTECTED access approvals expire automatically (seconds)
app.protected-access.ttlSeconds=3600

# OTP Configuration
app.otp.ttlSeconds=600
app.otp.maxAttempts=5

# Email (SMTP) Configuration - set these for OTP delivery
# Example (Gmail): host=smtp.gmail.com, port=587, starttls=true
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=manikandanmanikandan68519@gmail.com
spring.mail.password=ppoidjgeawhglktq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Optional explicit From address (defaults to spring.mail.username)
app.mail.from=manikandanmanikandan68519@gmail.com